export type HeroSlide = {
  id: string;
  image_url: string;
  image_width: number | null;
  image_height: number | null;
  sort_order: number;
  active: boolean;
  created_at: string;
  updated_at: string;
};
