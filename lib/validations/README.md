Zod schemas live here, one file per form (e.g. sign-up.ts, pet-profile.ts,
new-appointment.ts). We'll add these as we build each form, and validation
will run on both the client (instant feedback) and server (real security).
