// /account/pets — full pet list (may end up merged into the /account home page later)
export default function PetsListPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Pets</h1>
      <p className="mt-2 text-zinc-500">Placeholder — pet list.</p>
    </div>
  );
}
