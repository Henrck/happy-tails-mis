// /account/notifications — TBD whether we build this out (Josh is deciding)
export default function NotificationsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Notifications</h1>
      <p className="mt-2 text-zinc-500">Placeholder — pending decision on scope.</p>
    </div>
  );
}
